apiVersion: v1
kind: Service
metadata:
  name: {{ include "hometable.fullname" . }}-frontend
  labels:
    {{- include "hometable.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    {{- include "hometable.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: http
