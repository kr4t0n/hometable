{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hometable.serviceAccountName" . }}
  labels:
    {{- include "hometable.labels" . | nindent 4 }}
{{- end }}
